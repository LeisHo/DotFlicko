DOTFLICKO -- CHANGELOG
================================================================================

PROJECT START: 2026-09-10  -  fresh git history (see this entry's own note)

--------------------------------------------------------------------------------
2026-09-10

Initial build and first several rounds of iteration, across multiple
sessions in the same working day. This repo began as a repurposed
DickoClicko checkout; per explicit user decision (asked via AskUserQuestion
rather than assumed) the public GitHub history was started fresh -- a
single initial commit reflecting the actual DOTFLICKO-specific state, not
DickoClicko's own unrelated rope-physics history. Pushed to
github.com/LeisHo/DotFlicko as commit 3517a52 (387 files, 1118 insertions
in that commit alone -- the cumulative diff since the fresh initial commit
d1d3c96).

A concurrent Claude session built the ball-physics/hand-collision system
(gravity, wall bounce, 3-capsule finger collision against a per-frame-
annotated skeleton) and the skeleton-annotation tool
(scripts/active/flick-skeleton-annotator.html) in parallel with the work
described below -- their own session isn't represented in this entry's
own Added/Fixed/Verified detail, only reviewed and built on top of.

Added:
- Mouse-follow hand entity that continuously rotates to point its pre-drawn
  "up" orientation at the viewport's centerpoint, switching between 8
  directional animation sets (Behind, Behind Thumb, Side Thumb, Front
  Thumb, Front, Front Pinky, Side Pinky, Behind Pinky) based on the mouse's
  angle from center, in exact 45-degree sectors per the user's own spec.
- Click-to-play forward+reverse flick animation per direction, with a
  dev-tunable "Peak Frame Hold" (0-10 extra frames at the fully-extended
  pose before reversing).
- A §12-compliant dev panel, built from the workspace's own
  TEMPLATE_DEV_PANEL.html, with Entity/Background/Debug groups (settings
  decomposed per CLAUDE.md §12n).
- Per-direction Hand Rotation Offset: a dropdown + slider compound row (8
  independently-persisted values, not forgotten when switching the
  dropdown without saving) replacing an earlier single global offset.
- Top-edge ball collision, except during the ball's initial fall into frame
  from its off-screen-above spawn point (a deliberate exception so it isn't
  immediately bounced back out before ever being seen).
- A ball-proximity auto-trigger for the flick sequence: the sequence starts
  automatically (no click needed) when the ball enters the union of the
  hand's own collision capsules across every frame of the animation
  sequence, not just one pose -- per explicit correction from an initial,
  narrower "peak frame only" spec.
- Converted the physics loop from a raw per-real-frame delta to a fixed
  1/60s timestep accumulator, for reproducible ball motion given identical
  inputs (a static mouse position included).

Fixed:
- mouseX/mouseY/entityX/entityY could read/derive from
  window.innerWidth/innerHeight at 0 (a real early-page-life quirk,
  reproduced in this session's own testing, same class already documented
  elsewhere in this workspace for mobile Safari/Chrome) -- permanently
  stranding the entity off-screen. Fixed by deferring the real center-based
  initialization into the physics loop's own first live tick.
- The canvas element itself could get stuck at 0x0 CSS size from the same
  root cause, with no later correction -- besides breaking rendering/hit-
  testing, this is the likely cause of a real "Hide OS Cursor doesn't work"
  report, since a 0x0 element has no real screen area for `cursor:none` to
  apply to. Fixed with a per-frame self-healing size check.
- 2 stale dev-panel groups ("Hand Animation", "Mouse / Rotation" -- both
  renamed/consolidated away from mid-session) kept reappearing as empty
  ghost groups for anyone whose browser had already saved dev-panel
  settings referencing those names, since the generic panel engine
  correctly (by design) recreates any saved group it doesn't recognize as
  a real user-created one. Fixed with a small, one-time-per-load migration
  that moves their rows into Entity and removes the empty shell.

Verified:
- Entity rotation math against 2 independent geometric cases (mouse
  directly above / directly right of center) before trusting it.
- The per-direction rotation-offset persistence (switch away, switch back,
  Save, Reset, and a full page reload) via real event dispatches against
  the live DOM, not just code review.
- The ball-proximity auto-trigger via 4 separate live tests driving the
  real production functions with controlled inputs, including a case that
  only the corrected "any point in the sequence" (not "peak frame only")
  design could pass.
- The fixed-timestep determinism claim rigorously: 2 differently-jittered
  synthetic real-frame-timing sequences, compared at matching TOTAL FIXED-
  STEP COUNT (not wall-clock callback index), produced bit-for-bit
  identical ball position across all 192 directly-comparable steps.

--------------------------------------------------------------------------------
2026-09-10 (continued) -- spawn-placement-mode branch, uncommitted

A major architecture rework, proposed and explicitly approved by the user
before implementation (CLAUDE.md §0a) on a new `spawn-placement-mode`
branch, plus several follow-on refinement rounds in the same session.

Added:
- Spawn-and-place model, replacing the mouse-anchored entity entirely: the
  animation is invisible on load; a 2-click gesture (1st click = fixed
  anchor point, 2nd click = aim point) spawns a new, independently-tracked
  entity along that line, at whichever of the 8 directions the line's angle
  falls into. Multiple entities can be placed at any angle; each keeps its
  own play/collision/trigger state. Manual click-to-flick was retired
  (every click is now part of the spawn/adjust gesture) -- the ball-
  proximity auto-trigger, generalized to loop over every entity
  independently, is the only way any entity's flick fires.
- Entity anchor point changed from image centroid to the bottom-center of
  the frame's actual VISIBLE (non-transparent) content -- a one-time-per-
  direction alpha-channel scan (computeVisibleContentBounds), cached per
  direction. Confirmed real, non-trivial padding in the raw PNGs (e.g.
  'front': centerX=0.4765, bottomY=0.9145, not the naive 0.5/1.0).
- Swept (segment-vs-segment closest-approach) ball-vs-finger collision,
  replacing a point-only test -- fixes a fast ball tunneling clean through
  the hand between 2 physics ticks without ever registering a hit.
- Live drag preview: while aiming (between the 1st and 2nd click), the
  actual sprite renders translucently at the real live angle/direction, so
  the user sees exactly what will be placed before committing.
- Start/Stop buttons (always-visible UI, independent of the dev panel):
  Start lets balls spawn and fall; Stop halts spawning and clears any ball
  on screen immediately. Entity placement is gated to Stop mode only.
- Stop-mode entity adjustment: 2 draggable dots per entity (base = the
  fixed anchor point, end = the original aim point) plus a dashed
  connecting line. Dragging the base dot translates the whole entity
  (moves, keeps its rotation); dragging the end dot keeps the base fixed
  and rotates the entity in place, recomputing direction/rotation with the
  same formula the original 2-click spawn gesture uses (switching
  direction, and preloading its frames, if the drag crosses a 45-degree
  sector boundary).

Fixed:
- A freeze-on-first-click regression that the visible-content anchor scan
  itself introduced: computeVisibleContentBounds ran at full image
  resolution (measured 36-83ms per direction), and the new live drag
  preview called it every animation frame for whichever direction the
  mouse currently pointed toward -- sweeping across multiple direction
  sectors while aiming chained several of these expensive scans together,
  reading as a hang. Fixed by scanning a downscaled (200px) copy of the
  image instead of the original (measured: sub-1ms per direction after a
  one-time ~30-56ms warmup on the very first scan).

Verified:
- The spawn-placement rework end-to-end via real synthetic click sequences:
  correct direction bucket/rotation for 2 different line angles, entities
  staying fixed and independent of later mouse movement and of each other,
  per-entity trigger isolation (forcing the ball onto one entity's tip only
  flipped that entity's own playing flag), and that clicking an already-
  spawned entity does NOT trigger it (starts a new spawn pair instead).
- Both anchor-point changes visually (screenshot comparison, and a
  temporary debug-marker crosshair at the entity's own exact anchor point)
  and via the joint-collision debug lines, confirming the collision
  geometry stayed in sync with the sprite draw through both changes.
- The swept-collision algorithm in isolation (a fast ball crossing a
  segment mid-tick correctly resolved at the true crossing point, where
  the old point-only test would have missed it by 30px), then via a
  realistic end-to-end integration test (a 500px/tick ball correctly
  caught and resolved; the collision velocity kick still applies correctly
  using real per-tick joint-speed data).
- The live drag preview matches exactly what gets committed on the actual
  2nd click (same direction, same rotation, to 2 decimal places).
- The freeze fix via direct timing measurement (before/after) AND a full
  realistic interactive end-to-end test matching the user's own reported
  flow (click, sweep the mouse across 5 direction sectors via hover,
  confirm `document.readyState` stays "complete" throughout with no hang,
  2nd click completes the spawn correctly).
- Start/Stop and the draggable dots live: base-dot drag confirmed to
  translate x/y AND the end point by an identical delta with rotation/
  direction unchanged; end-dot drag confirmed to leave x/y fixed while
  rotation changed (screenshot-confirmed the hand visibly rotating in
  place); Start confirmed to hide the dots, disable itself, and spawn a
  falling ball; Stop confirmed to clear the ball immediately and re-show
  the dots; a click while running confirmed to spawn nothing.

Did not commit or push any of this branch's work -- stays local on
`spawn-placement-mode` until the user asks to merge/push.

--------------------------------------------------------------------------------
2026-09-10 (continued again) -- spawn-placement-mode branch, uncommitted

User report: "placing the entities still freezes" -- a regression report
against the freeze already fixed earlier the same day. Investigated fresh
rather than assuming the prior fix had failed.

Fixed:
- The dev panel (#devPanel) had no default-hidden state -- only a runtime
  toggle. On any genuinely fresh page load (no saved localStorage, which
  describes a real first-time visitor), the panel rendered wide open,
  covering most of the left/upper viewport including the Start/Stop
  buttons. A click meant to place an entity's first anchor point landed on
  the panel instead of the canvas -- nothing happened, indistinguishable
  from a freeze to a user with no reason to suspect an invisible overlay
  was eating their clicks. This session's own earlier verification never
  caught it because it reused an already-open browser tab where the panel
  had already been manually hidden once, and that hidden state persisted
  in the live DOM across reloads (not via localStorage) -- masking the
  bug for every test after the first. Fixed by adding the `hidden` class
  to `#devPanel` directly in the static HTML so it now boots hidden by
  default; the existing "DEV" corner button still opens it on demand.
- Added defensive try/catch around the visible-content anchor scan
  (computeVisibleContentBounds, inside visibleBoundsForDirection) as a
  precaution -- render() has no try/catch above it in the call stack, so
  any uncaught throw there (e.g. a possible canvas-taint SecurityError on
  some browsers for a file://-opened page) would silently kill the whole
  animation loop after the first placement, looking identical to a real
  freeze. Falls back to, and caches, the same existing 0.5/1 default on
  any throw. Not confirmed as the actual cause of this particular report
  (the dev-panel default explains it fully on its own) -- added as
  hardening, labeled honestly as such.

Verified:
- Reproduced against a genuinely fresh load (`localStorage.clear()` +
  reload, not the already-configured tab used for every earlier test) --
  confirmed via screenshot the panel now starts hidden with the canvas
  fully clickable, then confirmed a real 2-click placement sequence spawns
  an entity correctly with no freeze (`document.readyState` stayed
  "complete" throughout).

--------------------------------------------------------------------------------
2026-09-10 (continued again) -- spawn-placement-mode branch, uncommitted

3 requests: bake tuned Hand Rotation Offset defaults, fix a remaining
anchor-accuracy complaint, and add a new per-direction Animation X Offset
control.

Added:
- Hand Rotation Offset defaults updated to a newly-tuned set (behind: 0,
  behind-thumb: 2, side-thumb: 11, front-thumb: 5, front: 0, front-pinky:
  -5, side-pinky: -3, behind-pinky: -2), baked directly into
  cfg.entityRotationOffsetByDirection per the same "Set Hand Rotation
  Offset defaults" convention established earlier this project.
- Animation X Offset: a 2nd compound dropdown+slider dev-panel row (own
  dropdown, own 8 per-direction sliders), mirroring Hand Rotation Offset's
  own architecture. Lets each direction's sprite/collision placement be
  manually shifted perpendicular to its own placement line (%vmin,
  defaults 0/no-op) -- a local-x correction applied before rotation, so it
  always reads as sideways-relative-to-facing rather than a raw screen
  nudge, threaded through the same 4 call sites the visible-content anchor
  already has to stay in sync across. Never touches the entity's real
  clicked anchor/aim points (x/y/endX/endY), so the Stop-mode adjustment
  dots are unaffected.

Fixed:
- The visible-content anchor (added, then freeze-fixed via a downscaled
  scan, earlier the same day) was still measurably off after that freeze
  fix: the downscale itself (MAX_SCAN_DIM=200) introduced a real, non-
  trivial accuracy error (up to 0.0065 of image height, ~2-4px at typical
  scale, on the worst-observed direction) versus a full-resolution scan,
  confirmed via a direct empirical comparison across all 8 directions
  rather than assumed. Retuned MAX_SCAN_DIM to 400 after measuring several
  candidate values' own error/speed tradeoff -- cuts the error to ~0.0018
  (sub-1px) while the scan itself still costs only ~1.2ms, far short of
  freeze territory.

Verified:
- The rotation-offset defaults and the new X-offset row's own default (0
  for all 8 directions) confirmed live via direct cfg inspection after a
  fresh load.
- The MAX_SCAN_DIM retune via a temporary full-resolution reference
  implementation of the scan, compared numerically against the live
  production function across all 8 directions; a pixel-level readback of
  the actual rendered canvas (not just the source PNG) confirmed the
  anchor lands within 1-2px of the sprite's own true rendered bottom edge.
- The X Offset control's dev-panel row renders correctly (screenshot,
  after scrolling to it); a direct test (a large, deliberately visible
  offset value + Show Hitbox + Show Joint Collision Lines) confirmed the
  drawn sprite, its hitbox, and its collision debug line all shift
  together, staying in sync; a full real 2-click placement sweep after
  resetting test state confirmed no regression of the freeze fix.

--------------------------------------------------------------------------------
2026-09-10 (continued yet again) -- merged spawn-placement-mode into main,
pushed to origin/main as commit 3be0ac4

5 requests handled in one turn, several arriving mid-turn: a 3rd report
against the visible-content anchor ("still not at the base of the hand...
regardless of scale"), live Hand Rotation Offset for already-placed
entities, 2 more rounds of dev-panel default bakes, an angleOffset change,
and a new Delete mode. Session then merged and pushed the branch to main
per explicit request ("Then you can push as main").

Added:
- Live Hand Rotation Offset: moving the dev-panel slider for a direction
  now immediately rotates every already-placed entity of that direction.
  Entities no longer store a `rotationRad` field at all -- a new
  `entityRotationRad(entity)` recomputes it from the entity's own
  permanent `endX`/`endY` plus the CURRENT cfg offset every time it's
  needed (render, collision, the trigger-zone check), rather than a value
  frozen at spawn/drag time.
- Delete mode: a 3rd always-visible sim-control button. While active, a
  click on any placed entity's own sprite (a proper point-in-rotated-
  rectangle test against the same box Show Hitbox draws, not just the
  small anchor-dot radius used for dragging) removes it. Mutually
  exclusive with placement and dragging, gated to Stop mode the same way.
- `cfg.angleOffset` default changed to 180.
- 2 more rounds of Hand Rotation Offset / Animation X Offset default bakes
  from pasted Copy Settings dumps, continuing the tuning process.

Fixed:
- The visible-content anchor's real, root cause -- not another retune of
  the runtime scan's downscale dimension. Traced the "still not at the
  base of the hand... regardless of scale" report back to the defensive
  try/catch added during the earlier freeze fix: it could silently
  swallow ANY scan failure (with only a console.warn) and fall back to
  the OLD raw-PNG default, meaning neither the freeze fix's downscale nor
  its later accuracy retune could ever have mattered if that exception
  were firing in the user's real environment. Eliminated the entire
  runtime-scan failure surface instead of chasing it further: computed
  all 8 directions' visible-content bounds OFFLINE via a one-off Node
  script using the `sharp` package (already present in node_modules)
  directly against the source PNGs on disk, and baked the results as a
  static `VISIBLE_BOUNDS_BY_DIRECTION` lookup table -- deleting
  `computeVisibleContentBounds`/`visibleBoundsCache`/`MAX_SCAN_DIM`/the
  try-catch entirely. `visibleBoundsForDirection()` is now a synchronous
  table lookup with no image loading, no canvas, no getImageData, and
  therefore no way for any version of this failure mode to recur. The
  Node-measured values matched this session's own earlier in-browser
  full-resolution measurements to 4 decimal places on 7 of 8 directions.

Verified:
- The new static bounds table at 2 different entityScale values (87, 150)
  via a real rendered-canvas pixel readback, confirming the sprite's own
  true bottom edge lands within 1px of the anchor at both (a small
  discrepancy at an extreme small scale, 40, was separately attributed to
  the canvas's own image-smoothing/downscale blending at that render
  size, not the bounds data, which was independently corroborated correct
  via the Node measurement).
- The live rotation-offset link via direct before/after inspection of
  `entityRotationRad()` around a simulated slider change (0deg -> 45deg)
  on an already-existing entity, with no respawn.
- Delete mode end-to-end: the button toggles to a visibly active state, a
  click on a placed entity's own sprite removes it, toggling Delete back
  off and placing a fresh entity via the normal 2-click gesture
  immediately afterward worked with no interference between the 3
  mutually-exclusive Stop-mode interaction modes.
- The new dev-panel defaults via direct cfg inspection after a fresh load.

Merged/Pushed:
- Committed only this session's own 4 changed files (index.html,
  docs/CHANGELOG.txt, docs/CODE_SUMMARY.md, docs/PROJECT_PROGRESS.md) --
  explicitly NOT the ~552 other uncommitted working-tree changes present
  at the time (a rename/re-export of the SCISS/SNAP variant PNG asset
  sets, evidently another concurrent session's own in-progress work,
  confirmed untouched before and after via `git status`/`git diff --stat`
  on the exact file list staged). Discovered along the way: this repo's
  local `master` branch is stale leftover DickoClicko history unrelated
  to this project and was NOT used -- the real integration branch is
  `main`, which tracks `origin/main` (https://github.com/LeisHo/DotFlicko.git)
  and was at the same commit `spawn-placement-mode` had branched from, so
  merging was a clean fast-forward. Pushed `main` to `origin/main`
  (1cb92c5..3be0ac4).

--------------------------------------------------------------------------------
2026-09-10 (continued once more) -- git-tracked Save Settings, Delete
mode accuracy fix, further dev-panel default bakes

3 requests: "reference Clicko and DickoClicko and implement the non local
save" (Save Settings had been localStorage-only); "the delete button isnt
accurate... make sure the delete button only triggers when i am click
directly on teh visible part of the entity"; 2 more rounds of Hand
Rotation Offset / Animation X Offset default bakes from pasted dumps.

Added:
- Save Settings now writes to a git-tracked settings log
  (`data/processed/dev-panel-settings.json`), not just `localStorage` --
  ported from Clicko/DickoClicko's own already-proven 3-tier
  implementation (read directly from both projects' `index.html`/
  `api/save-settings.js` rather than re-derived from memory): Tier 1, a
  new `api/save-settings.js` Vercel serverless function committing via
  GitHub's Contents API (works from any device, including phones with no
  filesystem access); Tier 2, the File System Access API for a direct
  local disk write (desktop Chromium, local dev only); Tier 3, a download
  prompt + sessionStorage + the old `DEV_PANEL_LOCALSTORAGE_KEY` as a
  last resort. Reuses the exact same workspace-wide shared
  `DEV_PANEL_SAVE_SECRET` value Clicko/DickoClicko already use (confirmed
  identical in both their `index.html` source). Adapted (not a literal
  copy) to this project's own simpler snapshot shape:
  `captureFullDevPanelState()`'s existing single flat object is used
  as-is, since (unlike Clicko/DickoClicko) this project's
  `DEV_PANEL_LAYOUT` isn't split per device tab, so there's no separate
  per-tab geometry merging needed. Boot now calls `resetDevPanelSettings()`
  fire-and-forget (not awaited), matching Clicko/DickoClicko's own async
  boot pattern, since Tier 1's read is a real network fetch. Documented
  the required Vercel env var setup (`GITHUB_TOKEN`, `DEV_PANEL_SAVE_SECRET`)
  in README.md, mirroring DickoClicko's own README section.

Fixed:
- Delete mode deleted the WRONG entity whenever 2 placed entities' drawn
  bounding boxes overlapped (very common -- these sprites carry a lot of
  transparent padding) and a click meant for one entity's visible hand
  landed, within a different (topmost-checked) entity's own box, on a
  point that was transparent in THAT entity's own image. Added a real
  per-pixel opacity check (`getDirectionAlphaCanvas()`/
  `isOpaqueAtLocalPoint()`): a bounding-box hit now also requires the
  actual pixel at that point to be opaque in that entity's own frame-1
  image, checked against a per-direction cached full-resolution offscreen
  canvas (safe to draw at full res here, unlike the old removed anchor-
  point scan, since this only runs on a rare human-paced click while
  Delete mode is active, never per-frame) -- a box hit on a transparent
  pixel now falls through to check entities further underneath instead of
  stopping there.
- Hand Rotation Offset and Animation X Offset defaults re-baked twice more
  from newly pasted Copy Settings dumps as tuning continued (most recent:
  behind: 1, behind-thumb: 2, side-thumb: 0, front-thumb: 1, front: -1,
  front-pinky: 1, side-pinky: -1, behind-pinky: 0 for rotation; behind:
  -0.5, behind-thumb: -1.5, side-thumb: -0.5, front-thumb: 0.5, front: 0,
  front-pinky: 0, side-pinky: 0, behind-pinky: -0.5 for X offset).

Verified:
- The git-tracked Save Settings port's CLIENT-SIDE logic against the local
  test server (no real Vercel function behind it): `writeSettingsViaApi()`
  and `readSettingsViaApi()` both fail/return null gracefully (HTTP 501,
  then a 404-equivalent) rather than throwing; the full
  `loadDevPanelSettingsAsync()` chain falls through all 3 tiers to `null`
  cleanly with nothing saved anywhere; `getGitSettingsFileHandle('read',
  false)` returns null without prompting, as designed for a boot-time/
  Reset read. Did NOT verify the actual GitHub-committing behavior end-to-
  end against the real deployed Vercel site (no known deployment URL to
  test against in this session) -- flagged as an open item in
  PROJECT_PROGRESS.md for a session that can reach the live deployment.
- The Delete-mode fix directly: 2 entities placed 60px apart (heavily
  overlapping ~346px-wide bounding boxes) -- confirmed `hitTestEntitySprite()`
  now correctly resolves a click on either entity's own actual wrist/hand
  pixels to THAT entity specifically (not the topmost-checked one), and a
  click on empty background between/around both returns no hit at all;
  reproduced the exact reported symptom with a real click via the actual
  Delete button and confirmed the correct (clicked) entity was removed,
  leaving the other one intact.

--------------------------------------------------------------------------------
2026-09-10 (continued yet again) -- Ball Max Speed slider, dev-panel polish

3 small requests: "provide me a ball max speed slider"; "make the Add
Group button look the same style as the Copy Reset Save buttons"; "remove
the texxt behind the start stop delete buttons. not sure what that is."

Added:
- Ball Max Speed (%vmin/s) dev-panel slider (Ball group, default 300) --
  a hard cap on the ball's own speed, applied ONCE per tick, AFTER every
  velocity change that tick (gravity, wall bounce, the segment-reflection
  fix, the collision kick), so it consistently caps the result regardless
  of which source pushed the ball over, not just one of them.

Fixed:
- The "+ Add Group" button (all 3 device tabs) was a bare, unstyled
  `<button>` -- wrapped each in the same `.dev-buttons` container Copy/
  Reset/Save already use, so it now matches their exact look (accent
  color, height, font, and respects the Capitalize Button Text setting)
  instead of the browser's own default button style.
- The `showAngleDebug` status text ("entities: N ... [running/stopped]")
  rendered at a fixed (12,12) on the canvas -- directly behind the Start/
  Stop/Delete buttons (`.sim-controls`, top:10px/left:10px), a real,
  reported visual bug. Moved to (12,50), clear of the button row.

Verified:
- Ball Max Speed live: gave a test ball an extreme velocity (5000,5000)
  well past any reasonable cap, ran one real step(), confirmed the
  resulting speed exactly matched the configured cap (vmin-scaled).
- Both dev-panel fixes via screenshot: "+ ADD GROUP" now renders identical
  to COPY/RESET/SAVE; the debug status text renders cleanly below the
  button row with no overlap at any of the states checked.

--------------------------------------------------------------------------------
2026-09-10 (continued once more) -- re-synced skeleton annotations

"I updated teh joint annotations" -- a pasted, re-annotated
flick-skeleton.json dump from the other concurrent session's own
flick-skeleton-annotator.html tool.

Fixed:
- Normalized the pasted data (joint key order within each keyframe varied
  arbitrarily -- sometimes joint2 first, sometimes tip first -- and
  keyframe numbers weren't sorted) into this project's existing
  convention (knuckle/joint1/joint2/tip order, keyframes sorted
  numerically) via a small one-off Node script, rather than hand-editing
  ~70 keyframes across 8 directions. Cross-checked structurally against
  the OLD data first: confirmed all 8 directions and all 4 joint names
  present with no typos, and that every change was purely additive (6 of
  8 directions gained 2-3 new keyframes each; side-pinky and
  behind-pinky unchanged; zero keyframes removed anywhere) before
  trusting it enough to overwrite the live file.
- Synced the new data to BOTH `data/processed/flick-skeleton.json` (the
  pretty-printed source of truth) and the embedded `FLICK_SKELETON`
  literal in `index.html` (a single-line compact copy) -- this project's
  own documented convention (CODE_SUMMARY.md), since the annotator tool
  only ever writes the standalone JSON file, never the embedded literal.

Verified:
- The 2 files are byte-identical once parsed (`JSON.stringify(embedded)
  === JSON.stringify(file)`) after the sync.
- `interpolatedLocalPose()` produces a valid, non-NaN pose for every
  frame 1-20 of all 8 directions (160 checks, 0 issues) against the new
  keyframe set.
- Live: spawned a real entity, confirmed the joint-collision debug line
  tracks the visible finger correctly at frame 1 (idle) AND at frame 15
  (one of the newly-added keyframes, not present in the old data at
  all) -- the exact frame most likely to reveal a transcription mistake
  in the new data, since it has no prior keyframe to fall back to.

--------------------------------------------------------------------------------
2026-09-10 (continued yet again) -- mobile performance: DPR cap + pre-scaled
idle-sprite cache

"i tested the app out on mobile and the placing, rotating, moving of the
entities are very very laggy." No mobile hardware available to profile
directly -- addressed with 2 well-established canvas performance
practices rather than a single confirmed root cause.

Fixed:
- The canvas's own backing store was sized off the RAW
  `window.devicePixelRatio` -- many phones report 3 or higher, making the
  actual pixel buffer up to 9x larger than DPR 1 for the identical
  on-screen CSS size (3x width * 3x height), with every fillRect/drawImage
  this file does filling into that full buffer. Added `effectiveDpr()`
  (caps at 2) and routed BOTH `resizeCanvas()` and `update()`'s own
  self-healing size-mismatch check through it (they must agree, or the
  mismatch check would call `resizeCanvas()` -- a full backing-store
  reallocation -- every single frame).
- Every idle (non-playing) entity, AND the live spawn/drag preview, drew
  from the full-resolution source image (up to 2400x1181px per frame)
  every single frame, letting the browser rescale it down to on-screen
  size on every call -- exactly the 2 draw paths active while placing,
  rotating, or moving an entity, since nothing plays mid-flick in Stop
  mode. Added `getScaledIdleFrame(directionKey, size)`, caching one
  already-correctly-sized offscreen canvas per direction (keyed on
  `size`, the one value shared by every direction since `cfg.entityScale`
  is global) and drawing from that instead -- a near-1:1 blit rather than
  a scale-down on every frame. Deliberately NOT applied to a mid-flick
  PLAYING entity's own current frame (individually short-lived, not worth
  caching every one).

Verified:
- `effectiveDpr()` returns the correct capped value and the canvas's own
  width/height match it exactly.
- The idle-frame cache: 2 consecutive calls at the same `size` return the
  identical cached object; changing `cfg.entityScale` correctly
  invalidates and produces a new cached canvas.
- Live: an entity rendered from the cache is visually correct (dots,
  hitbox anchor unaffected), and dragging its end-point dot to rotate it
  still works correctly against the cached sprite.
- **NOT verified:** whether either fix actually resolves the reported lag
  on real mobile hardware -- this environment has no way to profile real
  mobile CPU/GPU performance, only to confirm the fixes are logically
  correct. Flagged as an open item in PROJECT_PROGRESS.md for the user to
  re-test on their own phone.

--------------------------------------------------------------------------------
2026-09-10 (continued once more) -- tap-and-drag placement (mobile)

"on mobile, when placing entities, i want to be able to click and drag as
well. Click for the first point, then drag to the second point."

Added:
- A press-drag-release placement gesture, alongside (not replacing) the
  original 2-separate-taps flow -- both commit through the exact same
  `spawnEntityFromLine()` call. `pointerdown` records `spawnPointerId`
  alongside `pendingSpawnStart` when starting a NEW pending spawn; the
  existing `pointerup` handler (previously only used to end a Stop-mode
  dot-drag) now also checks: if there's a pending spawn, the release's
  `pointerId` matches the press that started it, and the release moved
  more than `MIN_DRAG_PLACEMENT_DISTANCE` (24px, a deliberate tap-vs-drag
  threshold) from the start, commit the entity using the release position
  as the 2nd point. Below that distance -- an ordinary tap's own release,
  including normal touch jitter -- nothing happens, exactly as before,
  so the original 2-tap gesture (1st tap, then a genuinely SEPARATE 2nd
  tap) keeps working unchanged.

Verified:
- A plain quick tap (press+release at nearly the same point) leaves
  `pendingSpawnStart` active rather than auto-completing -- the original
  2-tap flow's own 1st-tap behavior is unaffected.
- A real 2nd separate tap still completes a placement normally.
- A single press-drag-release (simulated as one continuous mouse-drag)
  places an entity correctly in one motion, rotated to point along the
  drag direction, matching the live preview shown during the drag.
- The same press-drag-release gesture works correctly under the browser
  tool's own mobile/touch emulation (375x812 viewport with touch
  translation), not just as a mouse drag.
- No regression to the UNRELATED existing dot-drag (adjusting an
  already-placed entity's base/end point in Stop mode) -- since that
  flow never sets `pendingSpawnStart`, the new pointerup logic correctly
  no-ops during it (confirmed: dragging an entity's end dot still
  rotates it in place without spawning a stray new entity).

--------------------------------------------------------------------------------
2026-09-10 (continued once more) -- "Allow Placement While Running" debug
checkbox

"add a checkbox in the debug section. When turned on, I can place
entities regardless of the start and stop state."

Added:
- `cfg.allowPlacementWhileRunning` (Debug group checkbox, default off) --
  when on, both placement gestures (2-tap and press-drag-release) work
  regardless of Start/Stop state. Deliberately scoped to NEW placement
  only, per the request's own exact wording ("I can place entities") --
  Delete mode keeps its own unconditional Stop-only check untouched, and
  the dot-hit-test used to start dragging an EXISTING entity is forced
  to skip (return null) whenever `running` is true, so a click on an
  already-placed entity's own anchor point starts a NEW placement
  instead of grabbing that entity to adjust it.

Verified:
- Default (checkbox off) behavior is byte-for-byte unchanged: a
  press-drag-release while running still does nothing, exactly as
  before this change.
- With the checkbox on: the same drag gesture places a new entity
  correctly while running (screenshot-confirmed: entity rendered, ball
  actively bouncing at the same time).
- Delete mode stays correctly disabled while running regardless of the
  checkbox (the Delete button itself stays disabled via the existing
  `setRunning()` logic, untouched by this change).
- A synthetic pointerdown dispatched exactly at an existing entity's own
  anchor point, while running with the checkbox on, correctly started a
  NEW placement (`pendingSpawnStart` set) rather than grabbing that
  entity's dot (`draggingEntity` stayed false) -- confirms the scoping
  is real, not just a comment.

Noted, not part of this change: another concurrent session is actively
adding a "Scenes" (named, saved entity arrangements) feature to this
same `index.html` and `.claude/launch.json` (a 3rd alt dev-server
config) -- both left untouched by this session beyond what git already
shows as unavoidably co-committed by editing the same shared file.

--------------------------------------------------------------------------------

2026-09-11 -- fix: click-and-drag entity placement broken on mobile
touchscreens

"clicking and dragging to place entities doesnt work on mobile. check
some touch screen thing, or maybe its some chrome browser on touchscreen
thing"

Root cause: `#scene` (the canvas) had no `touch-action: none` -- every
other draggable element in this file (`.dev-header`, the dev-panel
resize edges/corners) already sets it, the canvas never did. Without it,
a real touchscreen's native gesture recognizer treats a press-drag on
the canvas as a pan/scroll attempt and hijacks the touch mid-gesture,
firing `pointercancel` instead of letting `pointermove`/`pointerup`
continue -- kills exactly the press-drag-release placement gesture while
a plain tap (not enough movement to trigger the browser's own pan
detection) keeps working, matching the report precisely.

Fixed:
- Added `touch-action: none` to `#scene`'s CSS rule.
- Added a `window` `pointercancel` handler (mirroring the existing
  `pointerup` handler's own cleanup) that clears
  `draggingEntity`/`draggingPoint`, and -- when the cancelled pointer
  matches `spawnPointerId` -- `pendingSpawnStart`/`spawnPointerId` too.
  Not part of the reported bug itself, but directly relevant to making
  the fix robust: without it, a genuine cancel that `touch-action: none`
  doesn't prevent (an OS interruption, an edge-swipe navigation gesture)
  would leave `pendingSpawnStart` stuck, silently blocking every future
  placement until an unrelated tap happened to land far enough away to
  complete the stale gesture.

Verified:
- `getComputedStyle(#scene).touchAction` confirmed `"none"` live in the
  browser tool's own mobile-viewport emulation (375x812).
- A full press-drag-release gesture dispatched as synthetic touch-type
  `PointerEvent`s (`pointerdown` -> `pointermove` x2 -> `pointerup`, 100px
  of movement) on the canvas completed end-to-end: `spawnEntityFromLine`
  fired, entity count went 0 -> 1, screenshot-confirmed the new entity
  rendered at the drag's end point.
- Syntax-checked all inline `<script>` blocks after both edits.

Explicit, disclosed limitation: a JS-dispatched `PointerEvent` never
goes through a real browser's native touch-gesture recognizer, so this
environment cannot reproduce the actual hijack-and-cancel failure mode
itself -- only code inspection (matching the same `touch-action: none`
convention already used by every other draggable element in this exact
file) identifies the root cause and fix with confidence. The user should
re-test on their actual phone to confirm the real-hardware fix.

--------------------------------------------------------------------------------

2026-09-11 (continued) -- new feature: Flick Intensity Multiplier from
placement line length

"when placing entities, the distance between the 2 points matters. from
the base up to 100% up the visible image, that will be '1'. The length
of the line is capped at 2X the length of the visible image. At full
length, it will be '2'. That is the range. These 2 numbers are a
multiplier to the flick intensity. So at 100% height or less, it will
just be 1, at 2X height, the flick intensity will be 2x."

Added:
- `VISIBLE_BOUNDS_BY_DIRECTION` extended with a `heightFrac` field per
  direction (visible-content height as a fraction of the full raster,
  bottomY - topY) -- computed the SAME way as the existing centerX/
  bottomY fields (Node + `sharp` against the real frame-1 PNGs, not
  guessed), from the exact same idle-frame source images. The table's
  own regeneration one-liner comment updated to compute and log all 3
  fields together.
- `flickIntensityMultiplierForLine(directionKey, startX, startY, endX,
  endY)` -- the shared formula: distance / (idle-frame visible height at
  the current Entity Scale), clamped to [1, 2]. Since the 1x-2x
  multiplier range maps 1:1 onto the 1x-2x distance-ratio range, the
  formula is just the clamped ratio itself.
- `entityFlickIntensityMultiplier(entity)` -- thin per-entity wrapper,
  reading the entity's own permanent endX/endY LIVE every call (same
  pattern as the existing `entityRotationRad()`), so it isn't frozen at
  spawn time -- a live Entity Scale change or a Stop-mode end-dot drag
  updates it immediately, with zero extra bookkeeping, exactly like
  rotation already does.
- Wired into the ball-collision kick as an ADDITIONAL factor alongside
  the existing global `cfg.collisionIntensity` slider (not a
  replacement): `ball.vx/vy += nx/ny * speed * cfg.collisionIntensity *
  flickIntensityMult`, computed once per entity per tick (not once per
  the 3 segments each entity has).
- A live `intensity X.XXx` readout added to the existing debug status
  text (gated by the same "Show Angle Debug" checkbox that already
  showed entity counts etc.) while placing (2-tap/drag preview) or
  adjusting (Stop-mode end-dot drag) -- lets the user see the exact
  value their gesture is setting, not just find out later from ball
  behavior.

Verified:
- Live in the browser: a 40px drag showed `intensity 1.00x` (well under
  the visible-height threshold); a 200px drag showed a scaled
  intermediate value; an extreme (~5000px) drag capped correctly at
  `intensity 2.00x`.
- Completed a real placement at the capped distance (`entities: 1`),
  then dragged that entity's own end dot back to a much shorter
  distance and confirmed the debug readout dropped from 2.00x back to
  1.00x immediately -- confirms the live-recompute-from-endX/endY
  behavior, not a value frozen at spawn.
- Syntax-checked all inline `<script>` blocks after every edit.

Explicit, disclosed limitation: did NOT independently stage a live ball-
collision hit to observe the resulting kick amplitude difference between
a 1x and 2x placement -- the multiplier's own formula and live-recompute
behavior were directly confirmed, and the multiplication site was
confirmed by direct code review to be correctly wired into the existing,
already-proven collision-kick line (same call site `cfg.collisionIntensity`,
Air Drag, and Ball Max Speed were each previously verified at), but the
end-to-end ball-physics amplification itself wasn't separately traced.

--------------------------------------------------------------------------------

2026-09-11 (continued) -- new feature: Win/Lose test buttons + sequence
state machine

"Add 2 buttons 'Win' and 'Lose'. They will be for me to test the win and
lose animations. In the J:\CLAUDE\PROJECTS\DICKOCLICKO\data\FLICK\2TONED,
Ive added 2 folders per animation type. So 16 Total. Each type has MF
folder and a TU folder. TU is for Win, MF is for Lose. We will be using
the frames within the FIST subfolders in the TU and MF folders. When
either button is clicked, every placed entity on screen will transition
into the appropriate animation sequence. In all sequences, frames will
be played sequentially until frame _013 is reached. At which point it
will hold this pose for a predetermined amount of time *D*Win/Lose Fist
Hold Duration*, afterwhich it will finish the sequence of frames. At
frame _24, the frame will once again be held for a duration *D*Win/Lose
Final Hold Duration*. Afterwhich, the entire sequence will be played in
reverse without any in between frame holding. Implement and push."

Interpreted "J:\...\DICKOCLICKO\..." as an asset SOURCE location, not
the project to implement in -- the request's own "every placed entity on
screen" is this project's (DOTFLICKO's) own established terminology, and
DICKOCLICKO's own architecture (a mouse-follow rope/hand toy, confirmed
via its own recent ETA History entries) has no "placed entities" concept
at all. Confirmed by directly listing both projects' own 2TONED folders:
DOTFLICKO's had no new folders; DICKOCLICKO's had exactly the described
16 (8 directions x _MF/_TU).

Added:
- 2 new buttons, Win and Lose, in the always-visible top-left button row
  (alongside Start/Stop/Delete/Ball) -- deliberately NOT gated by
  running/stopped and not a toggled "mode" like Delete/Ball, since
  testing the animation should work regardless of ball-sim state; each
  click is a one-shot trigger applied immediately to every currently-
  placed entity.
- Copied only the `FIST` subfolder (not the sibling `ARCH`/`NOFIST`
  variants also present, which this feature never uses) of all 16
  DICKOCLICKO folders into this project's OWN
  `data/FLICK/2TONED/<FOLDER>_MF|_TU/FIST/` -- 384 PNGs, ~15.2MB. This
  project is a static single-file app with no cross-project asset
  serving, so the files had to be physically copied in, not referenced
  by a DICKOCLICKO-relative path.
- Independently computed each of the 16 folders' own frame-1 visible-
  content anchor (centerX/bottomY) via the same offline Node+sharp
  technique this project's own VISIBLE_BOUNDS_BY_DIRECTION table already
  uses -- a real, checked difference from the normal flick asset family
  (1920x912 raster, not 2400x1181), so reusing the OLD anchor values
  would have been wrong. Win and Lose within the same direction measured
  out pixel-identical (their own frame 1 is a shared "pre-flick"
  starting pose before diverging, per the "PF" prefix both share) -- 1
  shared anchor per direction, not a duplicated pair, with a comment
  flagging that assumption for a future asset replacement to re-check.
- `triggerWinLoseSequence(type)` -- resets any in-progress normal flick
  on every entity first (Win/Lose fully takes over an entity's own
  rendering; the 2 states are mutually exclusive), then starts a 5-phase
  state machine per the exact spec: forward1 (frame index 0->12, i.e.
  _001->_013) -> holdFist (frozen at 12, for
  cfg.winLoseFistHoldDuration real seconds) -> forward2 (continues 12->23,
  i.e. _013->_024, NOT reset to 0) -> holdFinal (frozen at 23, for
  cfg.winLoseFinalHoldDuration) -> reverse (23->0 continuously, no
  holding at 12 this time -- "without any in between frame holding").
  Sequence ends and the entity returns to normal idle rendering once
  reverse reaches 0.
- 2 new dev-panel sliders, "Win/Lose Fist Hold Duration (s)" and
  "Win/Lose Final Hold Duration (s)" (both default 1s, 0-5 range) in a
  new "Win / Lose" group -- per the *D* shorthand, no existing group fit
  so a new one was created, matching this project's own established §12g
  convention.
- Deliberately reuses the existing FLICK_BASE_FPS/cfg.clickAnimSpeed
  frame-advance rate for the non-hold phases (no separate speed slider
  was requested).
- Deliberately does NOT touch ball-collision/joint physics for a Win/Lose-
  animating entity -- there's no skeleton/annotation data for this new
  asset family at all, and the request frames this as a visual test
  feature, not a physics-integrated one; a Win/Lose entity's collision
  geometry stays whatever its normal idle pose would produce
  (entityRealFrameNumber() already returns 1 whenever entity.playing is
  false, which it is throughout a Win/Lose sequence).

Fixed along the way: the new "Win / Lose" dev-panel group's controls
initially threw ("Cannot read properties of null (reading
'addEventListener')") because this project's dev-panel groups are
static HTML `.dev-section` blocks (findGroupContent() does NOT auto-
create a missing group) -- added the matching markup block alongside
the existing Ball/Collision/Debug ones.

Verified:
- Syntax-checked all inline `<script>` blocks after every edit.
- All 384 copied files present (`find`-counted), matching the 16x24
  expected total.
- Live in the browser: placed a real entity (synthetic drag gesture),
  clicked Win, confirmed all 24 of that entity's resolved direction's
  win-asset frames loaded with real 200 OK network responses (not 404s).
- **Verified the state-machine timing/frame-boundary logic EXACTLY
  correct via an isolated Node simulation of the extracted formula**
  (not reconstructed from memory -- copied verbatim): forward1 stops at
  precisely frame index 12 (_013); holdFist lasts exactly
  cfg.winLoseFistHoldDuration before continuing from 12 (not resetting);
  forward2 stops at precisely frame index 23 (_024); holdFinal lasts
  exactly cfg.winLoseFinalHoldDuration; reverse has NO intermediate stop
  at 12, decrementing straight through to exactly 0, at which point the
  sequence cleanly completes.
- Live in the browser (partial/qualitative only -- see limitation
  below): captured canvas.toDataURL() hashes across a click-and-wait
  sequence and got 3 distinct, internally-stable plateaus in the
  expected order (an initial frame, a 2nd different held frame, a 3rd
  different held frame), each different from the plain pre-trigger idle
  hash and from each other -- consistent with genuine phase transitions
  occurring, though NOT usable to verify exact timing (see below).

Explicit, disclosed limitation: this session's Browser pane was
reporting `document.hidden === true` throughout this task's testing
(confirmed directly, `tabs_context`/`document.hidden`), which throttles
this project's own requestAnimationFrame-driven fixed-timestep
accumulator -- a real, already-documented environment constraint (not
new to this task), not a code defect. This is why the live-browser test
above could only confirm qualitative phase-transition behavior, not
exact real-time hold durations -- the isolated Node simulation is what
actually proves the timing logic is correct. The user's own click-
through in a real, focused browser tab is the first genuinely real-time
test.

--------------------------------------------------------------------------------

2026-09-11 (continued) -- Win/Lose Anim Speed slider

"provide me a slider to control the animation speed of the win/ lose
animations"

Previously advanceWinLoseSequences() (the Win/Lose state machine added
last turn) reused the normal flick's own cfg.clickAnimSpeed for its
forward1/forward2/reverse frame-advance rate, since no separate speed
control had been requested at the time. Added a dedicated
cfg.winLoseAnimSpeed (new "Win/Lose Anim Speed (x)" slider in the
existing "Win / Lose" dev-panel group), defaulted to clickAnimSpeed's
own default/range (11.7, 0.1-14.04) so introducing this slider doesn't
silently change existing Win/Lose playback speed on first load. Swapped
both of advanceWinLoseSequences()'s own cfg.clickAnimSpeed reads
(forward1/forward2's increment, reverse's decrement) to this new field
-- the 2 hold-duration sliders (Fist/Final) are untouched, since a
hold's own duration is real time, not playback speed.

Verified: syntax-checked; live in the browser, confirmed the new
slider defaults to 11.7 and setting it to a different value leaves
sliderClickAnimSpeed's own value completely unchanged, confirming the 2
speeds are genuinely independent cfg fields, not aliased.

--------------------------------------------------------------------------------

2026-09-11 (continued) -- fix: entities flashing in one-by-one on first
Win/Lose click after a hard refresh

"on a hard refresh, the first time i hit Win and Lose. the app seems to
have ot load the entities one by one with the new frames. they each
flash in succession"

Root cause: Win/Lose frames are lazy-loaded (ensureWinLoseLoaded(),
same lazy pattern as the normal flick asset set) -- the FIRST time a
given direction is actually used, all 24 of its frames start a fresh
fetch. render()'s own draw dispatch for a Win/Lose entity only set
drawSource when its current frame's Image was already `.complete` --
with no fallback, an entity whose frame hadn't finished loading yet
drew NOTHING that tick. Since each placed entity's own direction fetches
independently and completes at its own network speed, entities blinked
into view one at a time as their own fetches happened to finish --
exactly the reported symptom.

Fixed:
- render()'s Win/Lose draw branch now falls back to the entity's own
  idle frame (getScaledIdleFrame() + visibleBoundsForDirection() -- the
  SAME source/anchor the plain idle branch already uses, not a mix of
  the 2 asset families) whenever its actual Win/Lose frame isn't ready
  yet, instead of leaving drawSource null. An entity now never goes
  blank -- worst case it keeps showing its own idle pose until the real
  frame streams in.
- spawnEntityFromLine() now also calls ensureWinLoseLoaded() for BOTH
  'win' and 'lose' the moment an entity is placed, not only once the
  Win/Lose button is actually clicked -- gives the network a head start
  for the common case (place entities, look them over, then click Win/
  Lose), directly reducing how often the fallback above is even needed
  (not a substitute for it -- a fast-enough click right after placement
  can still race the network either way).

Verified:
- Syntax-checked all inline <script> blocks after every edit.
- Live in the browser, fresh navigation (fresh JS state, so
  ensureWinLoseLoaded() genuinely started new fetches, not served from
  an already-populated in-memory cache): placed an entity, clicked Win
  immediately, entity remained visible (screenshot-confirmed) rather
  than going blank.
- Live in the browser, fresh navigation: placed an entity and NEVER
  clicked Win or Lose -- confirmed via read_network_requests that its
  own Lose (_MF) asset fetches were already firing anyway, proving the
  placement-time prefetch works independently of the render-side
  fallback.

--------------------------------------------------------------------------------

2026-09-11 (continued) -- re-sync updated Lose (MF) frames for 3
directions

"i updated the lose frames. update and push"

Compared DICKOCLICKO's own source folders against this project's
existing copy (diff -rq, all 16 folders checked) rather than assuming
which ones changed: BEHIND_MF, BEHIND PINKY_MF, and BEHIND THUMB_MF had
genuinely new content (new filenames/prefixes, not just touched
timestamps) -- the other 13 folders (5 Lose + all 8 Win) were untouched.
Still 24 frames each, same prefix-per-direction-differs convention.

Replaced the 3 folders' old-prefix files with the new ones (72 old
files removed, 72 new files added) and updated WIN_LOSE_ASSETS' own
`lose.prefix` for behind/behind-pinky/behind-thumb to match. Re-
computed (not assumed) each of the 3 directions' own visible-content
anchor against the NEW frame-1 art via the same offline Node+sharp
technique -- came back numerically identical to the prior values
(consistent with frame 1 still being the same shared "pre-flick"
starting pose noted when this asset family was first added; only the
LATER frames in each sequence were revised).

Verified: syntax-checked; live in the browser (fresh navigation),
placed a 'behind'-direction entity, clicked Lose, confirmed all 24 new-
prefix frames load with real 200 OK responses (not the old, now-
unreferenced prefix) and the entity renders correctly anchored
(screenshot-confirmed, base dot lines up with the sprite's own foot).

--------------------------------------------------------------------------------

2026-09-11 (continued) -- re-sync updated Win (TU) frames for
side-pinky/side-thumb

"I updated frames for SidePinky TU and SideThumb TU. updat and push"

Confirmed exactly these 2 of 16 folders had new content (SIDE
PINKY_TU, SIDE THUMB_TU -- new prefixes, still 24 frames each) via
diff -rq against all 16, not assumed. Replaced the 48 old-prefix files
with the new ones, updated WIN_LOSE_ASSETS' own `win.prefix` for
side-pinky/side-thumb to match. Re-computed (not assumed) both
directions' own visible-content anchor against the new frame-1 art --
came back numerically identical to the prior values, same "shared
pre-flick starting pose" pattern as the previous Lose re-sync.

Verified: syntax-checked; live in the browser (fresh navigation),
placed a side-pinky entity and a side-thumb entity, clicked Win,
confirmed all 48 new-prefix frames loaded with real 200 OK responses
and both entities rendered correctly anchored (screenshot-confirmed).

--------------------------------------------------------------------------------

2026-09-11 (continued) -- new feature: "Collision Only While Playing"
checkbox

"provide me a checkbox. when checked, my flick collision geometry does
not detect collisions when not playing an aimation sequence"

Added `cfg.collisionOnlyWhilePlaying` (new checkbox in the existing
"Collision" dev-panel group, default off = unchanged existing
behavior). When on, an entity's own finger-joint capsules (the 3
segments used for the ball collision response: position correction,
velocity reflection, the Flick Intensity kick) stop physically
colliding with the ball whenever that entity isn't currently playing a
flick (`!entity.playing`). Implemented as a single added condition on
the existing collision block's own `if` check
(`entity.jointWorld && (!cfg.collisionOnlyWhilePlaying || entity.playing)`)
-- no new state, no new per-tick computation.

Deliberately left the ball-proximity auto-trigger check
(`ballInFlickTriggerZone`) completely untouched -- it's a separate,
independent check in the same loop (a sibling statement, not nested
inside the gated block), so an idle entity keeps detecting the ball
close enough to start its own flick normally; only the tighter,
physical capsule collision is what's suppressed while idle.

Verified via manually-driven, fixed-timestep physics ticks rather than
waiting on the browser pane's own animation loop, which was found to be
fully suspended (not just throttled) while the pane is hidden --
temporarily exposed a `window.__testCollision` hook (grep-confirmed
removed after) exposing `step()`/ball state/cfg directly, then drove
240 ticks by hand: confirmed a baseline run (checkbox off) produces a
real deflection kick as the ball passes an idle entity's geometry (vx
0->108, vy flips from +315 to -440); with the checkbox on, the exact
same scenario showed ZERO collision response while `entity.playing`
was false, and the first real kick landed only 3 ticks after
`entity.playing` had already flipped true via the (unaffected)
proximity trigger -- confirmed with real logged tick numbers, not a
visual-only check.

--------------------------------------------------------------------------------
2026-09-11

Win/Lose test-sequence per-asset frame count fix, and recovery of 3
misplaced replacement animation-frame sets.

Fixed:
- WIN_LOSE_ASSETS' pause frame ("fist hold") was derived from a single
  GLOBAL frame count/index shared across all 16 direction+win/lose
  combinations. User replaced BehindThumb_TU's animation art with a
  48-frame version (previously 24) and reported the pause frame needed
  to move from frame 13 to frame 25 accordingly; a mid-turn follow-up
  added BehindPinky_TU as also updated to 48 frames. Generalized
  frameCount onto each WIN_LOSE_ASSETS win/lose sub-object individually,
  and made the pause/last frame indices per-entity
  (entity.winLoseFistIndex/winLoseLastIndex), derived via
  Math.floor(frameCount/2) at trigger time instead of a hardcoded global
  constant -- verified this reproduces the original 24->13 relationship
  exactly, so the other 14 unaffected combinations kept their existing
  behavior. Commits ce57167 (behind-thumb + the per-asset generalization,
  mostly captured by a concurrent session's own commit along the way)
  and 57ca97a (behind-pinky + front-thumb + the real asset files, see
  below).
- A follow-up "also updated FrontThumb_TU frames" request surfaced that
  all 3 of the user's replacement frame sets (BehindThumb_TU,
  FrontThumb_TU, BehindPinky_TU -- 48 frames each) had actually been
  saved into the sibling DICKOCLICKO project's data folder by mistake,
  never into DOTFLICKO itself -- caught via `ls` disk verification
  against the user's own claim (CLAUDE.md §0c), then confirmed directly
  by the user ("oh i was saving it the wrong location my bad"). Copied
  all 144 PNGs into this project's data/FLICK/2TONED/<FOLDER>_TU/FIST/
  folders (verified byte-identical to source via md5sum, and all 48
  frames present per set via a real per-filename existence check, not
  just a total count). The replacement art also uses a different
  filename convention per direction than the old 24-frame sets (e.g.
  "BehindThumb _ TU_001.png" vs. the old "2T -BT - PF_F..." prefix
  pattern) -- WIN_LOSE_ASSETS' win.prefix updated per direction to
  match. Old 24-frame files for these 3 directions were left in place on
  disk, unreferenced by the updated prefixes, rather than deleted as an
  unrequested cleanup.

Gotcha discovered and documented (see this project's own CLAUDE.md):
a PostToolUse:Edit hook auto-opens the just-edited index.html as a
`data:` URL preview in the Browser pane, which LOOKS like a normally
loaded page but can't resolve any relative asset path -- every image
silently failed to load (naturalWidth stayed 0, no console error) until
diagnosed via `location.href` and the pane was explicitly navigated to
the real localhost dev-server tab instead.

Verified via the JS console against the real localhost server (not the
data: preview): fistIndex/lastIndex correctly derive to 24/47 for all 3
of the 48-frame directions vs. 12/23 for unaffected control directions;
frame 1/24/48 all load successfully for all 3 directions; a
live-triggered win sequence correctly holds in the holdFist phase at
frameAccum 24 (frame 25) for behind-thumb.

--------------------------------------------------------------------------------

2026-09-11 (continued) -- cap the placement line's own visible length at
2 hand-lengths

"okay. also cap the actual length of the visible dotted line to 2x the
length of the visible part of the animation frame" / "IE 2 hand
lengths"

The Flick Intensity Multiplier (added earlier this session) already
saturated its own DERIVED value at 2x regardless of how far the raw
drag went, but the actual dashed placement line, the committed entity's
own endX/endY, and a Stop-mode end-dot drag could all still extend
arbitrarily far past that point. Added a SEPARATE cap on the real point
itself: `clampLineEndpoint(directionKey, startX, startY, rawEndX,
rawEndY)` pulls a raw end point in along the same angle to at most
`MAX_PLACEMENT_LINE_RATIO` (2) times that direction's own visible
content height (`visibleHeightPxForDirection()`, factored out of the
existing multiplier formula for this reuse). Direction/angle is always
resolved from the RAW, uncapped point first (bucketing depends only on
angle, never distance) -- the clamp never changes which direction an
entity resolves to, only how far out the stored/drawn point sits.

Applied consistently at all 3 places an aim/end point is set or drawn:
`spawnEntityFromLine()` (the committed entity's own endX/endY), the
Stop-mode end-dot `pointermove` handler, and `render()`'s live
placement preview (the dashed line's own drawn endpoint -- the sprite
preview itself only ever depended on angle, unaffected either way).

Verified live in the browser: dragged an extreme distance (4000px+)
during placement and confirmed the dashed preview line visibly stopped
well short of the cursor rather than following it off-screen; released
to commit the entity and confirmed its own end dot landed at that exact
same capped point (not the raw drag target); the debug intensity
readout stayed exactly 2.00x throughout, confirming the existing
multiplier's own saturation and the new visual cap agree. The 3rd site
(Stop-mode end-dot drag) uses the identical shared clampLineEndpoint()
call already proven correct at the other 2 sites; not separately
pixel-verified due to an imprecise manual dot-grab attempt, but
structurally identical to the 2 confirmed paths.

--------------------------------------------------------------------------------

2026-09-11 (continued) -- repurpose: placement-line multiplier now
controls flick animation speed, not ball-collision intensity

"okay instead of the intensity, make it control the hand animation
speed"

The multiplier a placement line's own length drives (1x at <=1 hand-
length, ramping to a capped 2x at 2 hand-lengths -- unchanged formula)
previously scaled the flick's own ball-collision-kick strength ("Flick
Intensity Multiplier"). Repurposed per explicit request to instead
scale that SPECIFIC entity's own flick animation PLAYBACK SPEED,
multiplicatively on top of the existing shared Hand Anim Speed slider
(not a replacement for it) -- the same "additional factor" pattern the
line length used for collision intensity before this change.

Renamed throughout for accuracy (logic itself unchanged, purely a
rename + relocation of where the result gets multiplied in):
`flickIntensityMultiplierForLine` -> `flickSpeedMultiplierForLine`,
`entityFlickIntensityMultiplier` -> `entityFlickSpeedMultiplier`. The
ball-collision-kick site (`ball.vx/vy += nx/ny * speed *
cfg.collisionIntensity`) is back to using ONLY the global Collision /
Flick Intensity slider, no per-entity variance. `step()`'s own
`entity.playFrameAccum` advancement now reads
`dt * FLICK_BASE_FPS * cfg.clickAnimSpeed * entityFlickSpeedMultiplier(entity)`.
The debug-text live readout relabeled from `intensity X.XXx` to
`speed X.XXx`. `clampLineEndpoint()` (the visible-line-length cap added
last turn) is untouched -- it doesn't reference "intensity" specifically
and still applies exactly the same way to the same 3 call sites.

Verified precisely via manually-driven, fixed-timestep ticks (same
approach as the collision-checkbox task, sidestepping the browser
pane's own suspended-while-hidden rAF loop): placed 2 entities, one
with a short (1x) line and one with a maximally-long (capped 2x) line,
triggered both flicks and ticked them in lockstep -- the 2x entity's
own frame index tracked almost exactly double the 1x entity's at every
sampled tick (e.g. 23 vs. 11 at tick 5, 46 vs. 23 at tick 10), and the
2x entity finished its entire play sequence and returned to idle well
before the 1x entity did. Grep-confirmed zero remaining references to
the old "flickIntensity"/"FlickIntensity" names anywhere in the file
after the rename.

--------------------------------------------------------------------------------

2026-09-11 (continued) -- re-sync updated Win (TU) frames for 5
directions, add per-type anchor override + fix a pre-existing asset gap

"J:\CLAUDE\PROJECTS\DICKOCLICKO\data\FLICK\2TONED\FRONT PINKY_TU\FIST
updated these frames" / (follow-up) "and these
J:\CLAUDE\PROJECTS\DICKOCLICKO\data\FLICK\2TONED\FRONT_TU\FIST"

Started with the 1 named folder (front-pinky), but per this project's
own established discipline for these re-sync tasks, re-scanned ALL 16
direction/type folders via diff -rq rather than trusting only the named
one -- found 3 MORE already differing (BEHIND PINKY_TU, BEHIND THUMB_TU,
FRONT THUMB_TU) that neither the user's own message nor a shallow check
would have caught. Investigated why: `index.html`'s own WIN_LOSE_ASSETS
table ALREADY referenced these 3 directions' new 48-frame prefixes (a
DIFFERENT concurrent session's own already-committed, already-pushed
work generalizing frame counts per-asset), but the matching PNG files
were never actually copied into this repo -- a real, live, currently-
broken gap (those 3 directions' Win sequences would 404 the moment
anyone tried them), not something introduced by this task. The user's
own follow-up message named a 5th folder (FRONT_TU) mid-task, added to
the same pass; a final full re-scan of the remaining folders confirmed
nothing else had changed.

Synced all 5 (72-96 files each, new prefixes, all now 48 frames except
front's own lose side and every other direction's own lose side, still
24): FRONT PINKY_TU, BEHIND PINKY_TU, BEHIND THUMB_TU, FRONT THUMB_TU,
FRONT_TU. Updated `index.html`'s own `win.prefix`/`frameCount` for
front-pinky and front specifically (the only 2 of the 5 whose CODE
side hadn't already been updated by the other session).

Re-computed (not assumed) all 5 directions' own visible-content anchor
against their new frame-1 art. 4 came back numerically identical to
the already-stored shared value (front, behind-pinky, behind-thumb,
front-thumb) -- but front-pinky's own NEW Win/TU frame 1 measured
genuinely different (centerX/bottomY 0.4846/0.9189 vs. the shared
0.4943/0.9221 every other still-unrevised entry uses), the first real
case this asset family has hit where win/lose diverge. Extended
WIN_LOSE_ASSETS to support an OPTIONAL per-type centerX/bottomY
override on the `lose`/`win` sub-object (falling back to the shared
direction-level value when absent) via a new `winLoseVisibleBounds()`
helper, used by render()'s own draw dispatch in place of a direct
`WIN_LOSE_ASSETS[key].centerX/bottomY` read -- added the override ONLY
for front-pinky's own `win` entry, since it's the only one a real
recompute actually showed differing.

Also corrected 2 now-stale CODE_SUMMARY.md GOTCHAS entries left behind
by the other concurrent session's own frameCount-generalization work
(the old shared `WIN_LOSE_FIST_INDEX`/`WIN_LOSE_LAST_INDEX` constants
they replaced with per-entity fields were still described as current).

Verified: syntax-checked after every edit; live in the browser (fresh,
cache-busted navigation to rule out a stale browser cache after an
initial confusing test result), confirmed front-pinky, front, and
BEHIND THUMB_TU (one of the 3 previously-broken directions) all
resolve correctly, fetch their real new frames with genuine 200 OK
responses, and render with correctly-anchored sprites
(screenshot-confirmed for front-pinky and front, base dot lining up
with the sprite's own foot in both cases).

--------------------------------------------------------------------------------

2026-09-11 (continued) -- re-sync updated Win (TU) frames for behind,
side-pinky, side-thumb

"also updated front and behind frames" / (follow-up) "as well as
SidePinky and SideThumb"

Full 16-folder `diff -rq` scan (per this project's own established
discipline, not trusting the message's own literal wording) found
exactly 3 actually differing: BEHIND_TU, SIDE PINKY_TU, SIDE THUMB_TU
-- "front" itself was already in sync from an earlier round this
session; the user's own follow-up named the other 2 before this task
otherwise would have needed to ask. None of these 3 directions' own
code entries had been touched by the other concurrent session (unlike
several directions in the previous round), so both the asset copy and
the `WIN_LOSE_ASSETS` prefix/frameCount update were this task's own to
do for all 3.

Synced: behind (48 frames, new prefix `Behind_TU`), side-pinky (new
prefix `DirctPink_TU`), side-thumb (48 frames, new prefix
`DirctThumbTU`). Re-computed (not assumed) all 3 directions' own
visible-content anchor against the new art -- all 3 came back
numerically identical to the already-stored shared value (same
"shared pre-flick pose" pattern confirmed several times now).

Found and DELIBERATELY did NOT try to fix a real gap in the SOURCE
itself: side-pinky's own Win/TU folder in DICKOCLICKO has only 43 real
files across a 001-048 numbering range -- frames 028-032 are simply
absent, not slow to load or corrupted. `frameCount` set to 48 anyway
(matching the highest real frame present, the established convention
for a still-in-progress source), and documented the gap directly in
`WIN_LOSE_ASSETS`'s own comment rather than fabricating replacement
frames or silently working around it -- the existing render()-side
idle-frame fallback (originally added for the cold-load-flash bug)
already handles a permanently-missing frame exactly like a still-
loading one, so this doesn't need a special case, just an honest note
for whoever next touches this table.

One real self-caught mistake during editing: initially wrote the
missing-frames comment on side-thumb's own entry instead of
side-pinky's (side-thumb's own 48 files are actually complete, no
gap) -- caught by re-checking my own earlier `ls`/gap-analysis output
against what I'd just written before moving on, not by a later test
failure; corrected before commit.

Verified live in the browser (cache-busted navigation): placed one
entity per affected direction (behind, side-pinky, side-thumb),
clicked Win, confirmed all 3 fetch their real new frame 1 with genuine
200 OK responses and render correctly anchored (screenshot-confirmed).
Final full 16-folder re-scan after all edits confirmed nothing else
had changed.

--------------------------------------------------------------------------------
2026-09-11 (2)

Added a 3rd Stop-mode entity-adjustment dot (yellow) as a dedicated
whole-entity move handle.

Added:
- A yellow dot, rendered exactly at the midpoint between a placed
  entity's existing base (blue) and end (pink) dots, draggable via the
  same pointerdown/pointermove/pointerup handlers as the other 2 --
  dragging it translates the whole entity (both points move together
  by an identical delta), preserving the line's own angle and length.

Fixed:
- Base (blue) and end (pink) dots now BOTH move fully independently
  when dragged, each pivoting on whichever one isn't being dragged.
  Previously only end moved independently (pivoting on base); base's
  own drag instead translated the whole entity -- that "move" behavior
  has moved to the new yellow midpoint dot above, per explicit request
  ("the 2 endpoints... will move independently... I want the opposite
  to be true" of end-only independence).

Verified via synthetic PointerEvents dispatched through the real
listeners (not direct state mutation) for all 3 drag modes, then a
real mouse-driven drag on the yellow dot in the browser pane visually
confirmed the same.

--------------------------------------------------------------------------------
<next entry follows the same divider + dated-header pattern>

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.
