DOTFLICKO -- PROJECT SUMMARY
================================================================================

Status: <one-line current state + date, e.g. "working research-prototype
pipeline (ingest -> detect -> export), as of YYYY-MM-DD".> See
PROJECT_PROGRESS.md for what's actively being worked on right now, and
CHANGELOG.txt for the full historical log -- this file stays a mid-altitude
snapshot, not a duplicate of either.

--------------------------------------------------------------------------------
OBJECTIVE

<What this project is for, in plain terms. What does it take as input, what
does it produce, who/what is it for. State explicitly what it deliberately
is NOT (e.g. "no frontend, no auth, no cloud APIs" if that's a real boundary)
-- a stated non-goal prevents a later session from silently expanding scope.>

--------------------------------------------------------------------------------
SCOPE

In scope: <what's actively maintained and iterated on.>

Out of scope / dormant: <anything deliberately excluded, deferred, or that
used to be part of this project but no longer is. If something was removed,
say so plainly rather than leaving stale references elsewhere to rot.>

Audience / how it's used: <single-user prototype? end-user-facing product?
internal tool? Say who actually uses this and how.>

--------------------------------------------------------------------------------
CURRENT STATE

<What's actually built and working right now, in enough detail that someone
could verify the claim. Reference real numbers where meaningful (test counts,
commit counts, data volume) -- per CLAUDE.md §0c, no rounding, state what was
actually measured.>

--------------------------------------------------------------------------------
DECISIONS

<Real architectural/scope decisions made and why -- especially ones that
would look wrong out of context, or that a later session might otherwise
"fix" without knowing they were deliberate. One bullet per decision:>

- <Decision> -- <why, and what it was chosen over.>

--------------------------------------------------------------------------------
DATA SOURCES

<Where the project's data comes from. Omit this section entirely for
projects with no meaningful external/acquired data.>

--------------------------------------------------------------------------------
KNOWN LIMITATIONS

<Honest, current list of what doesn't work yet, what's untested, what's a
known gap. This is a living list -- remove an item once it's actually
resolved, don't leave it here as stale history.>

--------------------------------------------------------------------------------
NEXT ACTION

<The single most concrete next step, or "no specific next action is
currently pending" if genuinely true. Keep this in sync with
PROJECT_PROGRESS.md's "What's next" section rather than letting the two
drift apart.>
